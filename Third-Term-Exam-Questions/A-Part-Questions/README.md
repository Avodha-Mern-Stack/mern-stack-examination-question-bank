
**PART A Questions**

1. Which hook is used to perform side effects in React functional components?
a. useState()
b. useEffect() ✓
c. useRef()
d. useMemo()

1. What is used to pass data from parent to child components in React?
a. Context
b. Props ✓
c. State
d. Redux

1. Which method updates state in React class components?
a. setState() ✓
b. updateState()
c. changeState()
d. modifyState()

1. What does JSX stand for?
a. JavaScript XML ✓
b. JavaScript Extension
c. JSON XML
d. Java Syntax Extension

1. Which lifecycle method is called after a component renders?
a. componentDidMount() ✓
b. componentWillMount()
c. componentWillUpdate()
d. shouldComponentUpdate()

1. How do you create a ref in React?
a. useRef() ✓
b. createRef()
c. Both a and b ✓
d. ref()

1. Which is used for conditional rendering in JSX?
a. if statements
b. switch statements
c. Ternary operators ✓
d. for loops

1. What is the purpose of keys in React lists?
a. Styling elements
b. Identifying elements uniquely ✓
c. Sorting elements
d. Filtering elements

1. Which hook replaces componentDidMount in functional components?
a. useEffect with empty dependency array ✓
b. useState
c. useCallback
d. useMemo

1.  How do you pass a function as a prop?
a. function={myFunction} ✓
b. prop={myFunction()}
c. func={myFunction()}
d. execute={myFunction}

**Redux & State Management (Questions 11-20)**

11. Redux stores the application state in:
a. Components
b. Actions
c. Store ✓
d. Reducers

12. What is the purpose of a reducer in Redux?
a. Make API calls
b. Handle side effects
c. Update state based on actions ✓
d. Dispatch actions

13. Which function creates a Redux store?
a. createStore() ✓
b. makeStore()
c. buildStore()
d. newStore()

14. What is an action in Redux?
a. A function that updates state
b. An object describing what happened ✓
c. A component
d. A middleware

15. Which method connects React component to Redux store?
a. connect() ✓
b. link()
c. attach()
d. join()

16. What does middleware do in Redux?
a. Handles asynchronous actions ✓
b. Renders components
c. Creates reducers
d. Styles components

17. Which package provides React bindings for Redux?
a. react-redux ✓
b. redux-react
c. react-with-redux
d. redux-components

18. What is an action creator?
a. A function that returns an action ✓
b. A component that dispatches actions
c. A reducer that creates actions
d. A middleware that generates actions

19. Where is initial state defined in Redux?
a. In the action
b. In the reducer ✓
c. In the component
d. In the middleware

20. What is Redux Thunk used for?
a. Async operations ✓
b. Synchronous state updates
c. Component rendering
d. CSS styling

**Node.js & Express (Questions 21-40)**

21. Which method handles GET requests in Express?
a. app.get() ✓
b. app.post()
c. app.put()
d. app.delete()

22. What does middleware do in Express?
a. Handle requests and responses ✓
b. Connect to database
c. Render views
d. Manage sessions

23. How do you start a Node.js server?
a. node server.js ✓
b. start server.js
c. run server.js
d. execute server.js

24. Which package manages dependencies in Node.js?
a. npm ✓
b. npx
c. yarn
d. Both a and c ✓

25. What does req.params contain in Express?
a. Query parameters
b. Route parameters ✓
c. Request body
d. Headers

26. Which method sends a JSON response in Express?
a. res.json() ✓
b. res.sendJSON()
c. res.ajax()
d. res.data()

27. How do you parse incoming JSON in Express?
a. app.use(express.json()) ✓
b. app.use(express.text())
c. app.use(express.urlencoded())
d. app.use(express.parse())

28. What is package.json used for?
a. Project metadata and dependencies ✓
b. Server configuration
c. Database connection
d. API documentation

29. Which method is used for error handling middleware in Express?
a. Four parameters (err, req, res, next) ✓
b. Three parameters (req, res, next)
c. Two parameters (req, res)
d. Five parameters

30. What does CORS middleware enable?
a. Cross-Origin Resource Sharing ✓
b. Cookies
c. Caching
d. Compression

**MongoDB & Mongoose (Questions 41-60)**

31. Which method finds documents in MongoDB?
a. find() ✓
b. search()
c. query()
d. get()

32. What is Mongoose?
a. MongoDB object modeling for Node.js ✓
b. A styling library
c. A testing framework
d. A deployment tool

33. Which method creates a new document?
a. create() ✓
b. insert()
c. add()
d. new()

34. What is a schema in Mongoose?
a. Database structure definition ✓
b. API endpoint
c. React component
d. CSS style

35. How do you update a document in Mongoose?
a. findByIdAndUpdate() ✓
b. updateDocument()
c. modify()
d. change()

36. Which method removes documents?
a. deleteMany() ✓
b. removeAll()
c. clear()
d. drop()

37. What does populate() do in Mongoose?
a. Replaces references with actual documents ✓
b. Deletes documents
c. Creates indexes
d. Validates data

38. Which method saves a document to database?
a. save() ✓
b. store()
c. insert()
d. commit()

39. What are MongoDB collections equivalent to in SQL?
a. Tables ✓
b. Databases
c. Rows
d. Columns

40. Which operator is used for filtering in MongoDB queries?
a. $match ✓
b. $filter
c. $where
d. $select

**REST API & HTTP (Questions 61-80)**

41. Which HTTP method creates a resource?
a. POST ✓
b. GET
c. PUT
d. DELETE

42. What does PUT method typically do?
a. Replace a resource ✓
b. Create a resource
c. Partially update
d. Delete a resource

43. Which status code indicates success?
a. 200 ✓
b. 400
c. 404
d. 500

44. What does 404 status code mean?
a. Not Found ✓
b. Bad Request
c. Server Error
d. Unauthorized

45. Which header indicates response content type?
a. Content-Type ✓
b. Accept
c. Authorization
d. Content-Length

46. What is REST?
a. Architectural style for APIs ✓
b. Programming language
c. Database
d. Framework

47. Which method is idempotent?
a. GET ✓
b. POST
c. PATCH
d. None

48. What does CRUD stand for?
a. Create, Read, Update, Delete ✓
b. Create, Render, Update, Destroy
c. Call, Read, Update, Delete
d. Create, Request, Update, Delete

49. Which tool is commonly used to test APIs?
a. Postman ✓
b. Photoshop
c. Excel
d. Word

50. What does JWT stand for?
a. JSON Web Token ✓
b. JavaScript Web Token
c. JSON Web Tool
d. JavaScript Web Tool

**JavaScript & ES6+ (Questions 81-100)**

51. Which is NOT a JavaScript data type?
a. String
b. Number
c. Boolean
d. Float ✓

52. What does const mean?
a. Block-scoped constant ✓
b. Function-scoped variable
c. Global variable
d. Temporary variable

53. Which method creates a new array from another?
a. map() ✓
b. forEach()
c. loop()
d. iterate()

54. What is the spread operator?
a. ... ✓
b. =>
c. &&
d. ||

55. Which handles asynchronous operations?
a. async/await ✓
b. for loops
c. if statements
d. switch cases

56. What does promise represent?
a. Future value ✓
b. Current value
c. Past value
d. No value

57. Which is used for array destructuring?
a. [] ✓
b. {}
c. <>
d. ()

58. What does === operator check?
a. Value and type ✓
b. Only value
c. Only type
d. Neither

59. Which method adds to array end?
a. push() ✓
b. pop()
c. shift()
d. unshift()

60. What is template literal syntax?
a. `text ${variable}` ✓
b. "text" + variable
c. 'text' + variable
d. "text ${variable}"

**React Router, Context API, Hooks (61-70)**

61. Which component is used to wrap routes in React Router v6?
a. BrowserRouter ✓
b. Router
c. RouteWrapper
d. Switch

62. What is the purpose of useContext() hook?
a. Access context values in functional components ✓
b. Create new contexts
c. Update context providers
d. Replace Redux entirely

63. How do you navigate programmatically in React Router v6?
a. useNavigate() hook ✓
b. history.push()
c. Redirect component
d. useHistory()

64. Which hook optimizes performance by memoizing functions?
a. useCallback() ✓
b. useMemo()
c. useRef()
d. useEffect()

65. What does useMemo() return?
a. Memoized value ✓
b. Memoized function
c. DOM reference
d. Side effect

66. Which is the correct way to create a context?
a. React.createContext() ✓
b. new Context()
c. createContextAPI()
d. useContext()

67. In React Router, what component renders content for a route?
a. Route with element prop ✓
b. Render component
c. Component prop
d. Children prop

68. Which hook replaces shouldComponentUpdate()?
a. React.memo() with custom comparison
b. useMemo()
c. useCallback()
d. Both a and b ✓

69. How do you access URL parameters in React Router v6?
a. useParams() ✓
b. match.params
c. route.params
d. useRoute()

70. What is the purpose of useReducer()?
a. Complex state logic with reducers ✓
b. Simple state management
c. API calls
d. DOM manipulation

**Authentication, Authorization, Security (71-80)**

71. Which is the most secure way to store tokens in frontend?
a. HTTP-only cookies ✓
b. localStorage
c. sessionStorage
d. Redux store

72. What does CORS protect against?
a. Cross-site request forgery
b. Cross-site scripting
c. Cross-origin resource sharing issues ✓
d. SQL injection

73. Which npm package is commonly used for password hashing?
a. bcrypt ✓
b. crypto
c. hash
d. encrypt

74. What is JWT used for?
a. Stateless authentication ✓
b. Database queries
c. CSS styling
d. State management

75. Which header should be set for JWT authentication?
a. Authorization: Bearer <token> ✓
b. Auth: <token>
c. Token: <token>
d. JWT: <token>

76. What does OAuth provide?
a. Third-party authentication delegation ✓
b. Password hashing
c. Database encryption
d. File storage

77. Which middleware helps prevent XSS attacks in Express?
a. helmet ✓
b. cors
c. morgan
d. compression

78. What should never be stored in JWT tokens?
a. Sensitive user data ✓
b. User ID
c. Expiration time
d. Issuer information

79. Which is a common authentication strategy?
a. Passport.js ✓
b. Express-auth
c. Node-authenticate
d. Mongo-auth

80. What does CSRF token protect against?
a. Cross-site request forgery ✓
b. Cross-site scripting
c. Clickjacking
d. SQL injection

**Testing, Deployment, DevOps (81-90)**

81. Which library is commonly used for React testing?
a. Jest with React Testing Library ✓
b. Mocha
c. Jasmine
d. Cypress

82. What does CI/CD stand for?
a. Continuous Integration/Continuous Deployment ✓
b. Code Integration/Code Deployment
c. Continuous Inspection/Continuous Delivery
d. Complete Integration/Complete Deployment

83. Which platform is popular for hosting MERN applications?
a. Heroku ✓
b. AWS
c. Azure
d. All of the above ✓

84. What is the purpose of .env files?
a. Store environment variables ✓
b. Store database credentials
c. Store API keys
d. All of the above ✓

85. Which command runs tests in Create React App?
a. npm test ✓
b. npm run test
c. npm testing
d. npm check

86. What does Docker provide?
a. Containerization ✓
b. Virtualization
c. Compilation
d. Minification

87. Which service is used for MongoDB cloud hosting?
a. MongoDB Atlas ✓
b. AWS DocumentDB
c. Azure Cosmos DB
d. All of the above ✓

88. What is the purpose of Git in deployment?
a. Version control and deployment automation ✓
b. Database management
c. API testing
d. CSS compilation

89. Which tool monitors application performance?
a. New Relic ✓
b. Webpack
c. Babel
d. ESLint

90. What does PM2 provide for Node.js?
a. Process manager for production ✓
b. Testing framework
c. Database ORM
d. API documentation

**Best Practices, Patterns, Architecture (91-100)**

91. Which pattern separates business logic from presentation?
a. Container/Presentational components ✓
b. MVC
c. Singleton
d. Observer

92. What is code splitting used for?
a. Reduce initial bundle size ✓
b. Increase code complexity
c. Improve database queries
d. Enhance security

93. Which improves React application performance?
a. Lazy loading components ✓
b. Using inline styles
c. Multiple re-renders
d. Large bundle sizes

94. What is the single source of truth principle?
a. State stored in one place ✓
b. Multiple state stores
c. Distributed state
d. No state management

95. Which is a good practice for API calls?
a. Centralized API service layer ✓
b. API calls in every component
c. Hardcoded URLs throughout
d. No error handling

96. What does DRY stand for?
a. Don't Repeat Yourself ✓
b. Do Repeat Yourself
c. Data Repeat Yourself
d. Design Repeat Yourself

97. Which improves database performance?
a. Indexing frequently queried fields ✓
b. No indexing
c. Large collections
d. Complex nested documents

98. What is microservices architecture?
a. Independent, loosely coupled services ✓
b. Monolithic application
c. Single database
d. Shared codebase

99. Which helps maintain code quality?
a. ESLint and Prettier ✓
b. No code reviews
c. Ignoring warnings
d. No documentation

100. What is the benefit of using TypeScript?
a. Static typing and better tooling ✓
b. Faster runtime
c. Smaller bundle size
d. Easier learning curve

---
